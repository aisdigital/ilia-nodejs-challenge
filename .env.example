# Shared JWT secret used by BOTH services
# Per challenge requirement, this must be ILIACHALLENGE.
JWT_PRIVATE_KEY=ILIACHALLENGE

# Optional internal key for service-to-service auth (if/when used)
INTERNAL_JWT_PRIVATE_KEY=ILIACHALLENGE_INTERNAL

# Optional: service ports (if your apps read them)
USERS_PORT=3002
WALLET_PORT=3001

# Example DB / RabbitMQ URLs (Docker Compose hostnames)
USERS_DATABASE_URL=postgres://postgres:postgres@users-db:5432/users
WALLET_DATABASE_URL=postgres://postgres:postgres@wallet-db:5432/wallet
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672
