version: "3.9"
services:
  mongo:
    image: mongo
    ports:
      - 27017:27017

  wallet:
    build: ./wallet/
    environment:
      - HOSTDB=mongo
      - PORTDB=27017
      - JWT_KEY=${JWT_KEY}
    ports:
      - "3001:3001"
    depends_on:
      - mongo
