#!/usr/bin/env node
// DEPRECATED: este wrapper foi removido. O comando atual de build usa o binário diretamente via npm script.
// Use `npm run build:users` para compilar.
console.log('Aviso: este script está depreciado. Remova o arquivo se não for mais necessário.');
process.exit(0);
